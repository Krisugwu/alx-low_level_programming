#include "main.h"
/**
 * _strlen - return lenght of a string
 * @s:input of string
 * Return: lenght of string
 */
int _strlen(char *s)
{
int count = 0
{
while (*(s + count) != '/0')
	count++;
return (count);
}
