#include "main.h"
/**
 * print_most_numbers - prin a number except 2 and 9
 *
 * Return: no return
 */
void print_most_numbers(void)
{
int ch = 0;
for (ch <= 9; ch++)
{
if (ch == 2 || ch == 4)
{
continue;
}
else
{
_putchar(x + '0')
}
}
_putchar('\n');
}
