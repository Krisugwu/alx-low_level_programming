#include "main.h"
/**
 * _isupper - check for uppercase character
 * @c: character out put
 * Return: 1 if its uppercase else return 0
 */
int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}
else
{
return (0);
}
}
